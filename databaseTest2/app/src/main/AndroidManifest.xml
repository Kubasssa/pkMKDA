<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kuba.databasetest">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="FitFreak"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".oldclasses.zClassUnused01" />
        <activity android:name=".oldclasses.zClassUnused02" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ActivityRegistration" />
        <activity android:name=".ActivityEditProfile" />
        <activity android:name=".ActivityAddNewProduct" />
        <activity
            android:name=".oldclasses.zClassUnused00"
            android:label="@string/title_activity_add_food" />
        <activity
            android:name=".oldclasses.ActivitySetPreference"
            android:label="@string/title_activity_set_preference"></activity>
    </application>

</manifest>